<table class="table">
  <thead class="thead-inverse">
    <tr>
      <th>#</th>
      <th>Name</th>
      <th>email</th>
    </tr>
  </thead>
   <tbody>
    <% if @users.any? %>
    <% @users.each do |user| %>
      
      <tr class="<%= 'info' if user.try(:admin?) %>">
        <td><%= user.id %></td>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td>X</td>
      </tr>
      
    <% end %>
    <% end %>
  </tbody>
</table>