<!DOCTYPE html>
<html>
<head>
  <title><%= full_title(yield(:title)) %></title>
  <%= csrf_meta_tags %>
</head>
<body>
  <%= render 'layouts/header' %>
  <div class="container">
  <div class="row">
    <%= render 'shared/flash' %>
    <% if @categories && !current_user.try(:admin?) %>
      <%= render 'layouts/catnav' %>
    <% end %>
  </div>
    <div class="row"><%= yield %></div>
    <%= render 'layouts/footer' %>
  </div>
</body>
</html>